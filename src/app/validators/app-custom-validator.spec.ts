import { AppCustomValidator } from './app-custom-validator';

describe('AppCustomValidator', () => {
  it('should create an instance', () => {
    expect(new AppCustomValidator()).toBeTruthy();
  });
});
 